import React from "react";
import styled from "styled-components";

const StyledHeader = styled.header`
  background: #171010;
  color: #d8c0c0;
`;

const Header = () => {
  return <StyledHeader>Photo Tagging Game</StyledHeader>;
};
export default Header;
